/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package statement_demo;
import java.sql.*;
/**
 *
 * @author admin
 */
public class Statement_Demo {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        
          try {
            //1. Register the driver
            Class.forName("com.mysql.cj.jdbc.Driver");
            
            //2. establish connection by con object
            
            Connection con=DriverManager.getConnection("jdbc:mysql://localhost:3306/paga","root","");

            System.out.println("Connection established");
            
            //3. Create the statement object which is 
            //used to execute query in database
            
                Statement stmt=con.createStatement();  
  
            //step4 execute query  

                ResultSet rs=stmt.executeQuery("select * from name");  
                 String sql1 = "update name set id=799 where id=399";
                  int rows = stmt.executeUpdate(sql1);
                    System.out.println("Rows impacted : " + rows );
          
                    System.out.println("Insert Record");
        PreparedStatement myStmt1=con.prepareStatement("insert into name values(?,?,?)");  
        myStmt1.setInt(1,101);//1 specifies the first parameter in the query  
        myStmt1.setString(2,"tc5");  
        myStmt1.setString(3,"Rajkot");  
  
        int i=myStmt1.executeUpdate();  
        System.out.println(i+" records inserted");  
        
                while(rs.next())  
                {
                    System.out.println(rs.getInt(1)+"  " +rs.getString(2)+"  " +rs.getString(3));  
                    
                }
                
            //5.close the connection
            
            con.close();
            
        } catch(Exception e) {
        }
    }
    
}